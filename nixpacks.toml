[providers]
python = "3.12"

[phases.setup]
nixPkgs = ["python312", "pip"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn ivor_semantic_search.api:app --host 0.0.0.0 --port $PORT"
